\begin{figure}[htbp]
    \centering
    \begin{tikzpicture}[
        node distance=0.7cm and 2cm,
        box/.style={
            draw,
            rectangle,
            thick,
            minimum width=3cm,
            minimum height=1.2cm,
            align=center
        },
        smallbox/.style={
            draw,
            rectangle,
            thick,
            minimum width=3.2cm,
            minimum height=0.95cm,
            align=center
        },
        botharrow/.style={
            <->,
            >=Latex,
            thick,
            rounded corners=2pt
        }
    ]
        % obere Reihe
        \node[box] (manager) {Manager};
        \node[box, left=of manager] (user) {\textbf{User}};
        \node[smallbox, right=of manager] (output) {output};

        % rechte Spalte
        \node[smallbox, below=0.6cm of output] (discovery) {testDiscovery};
        \node[smallbox, below=0.5cm of discovery] (auditor) {\textit{testAuditor}};
        \node[smallbox, below=0.5cm of auditor] (execution) {testExecution};

        % links: User <-> Manager
        \draw[botharrow] (user.east) -- (manager.west);

        % rechts: Manager <-> Komponenten, mit verschiedenen Startpunkten
        \draw[botharrow] ([yshift=12pt]manager.east) -- ++(1.6,0) |- (output.west);
        \draw[botharrow] ([yshift=4pt]manager.east) -- ++(1.2,0) |- (discovery.west);
        \draw[botharrow] ([yshift=-4pt]manager.east) -- ++(0.8,0) |- (auditor.west);
        \draw[botharrow] ([yshift=-12pt]manager.east) -- ++(0.4,0) |- (execution.west);

    \end{tikzpicture}
    \caption{Schematische Darstellung der Interaktion von User, Manager, \textit{testAuditor} und Hilfskomponenten.}
    \label{fig:tool-overview}
\end{figure}